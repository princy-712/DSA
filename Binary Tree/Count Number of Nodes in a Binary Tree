// User function Template for Java

class Solution {

    public static int countNodes(Node root) {
        // Code here
        if(root == null) return 0;
        
        int leftcnt = countNodes(root.left);
        int rytcnt = countNodes(root.right);
        return 1+ leftcnt+ rytcnt;
    }
}