class Solution:
    def twoSum(self, arr, target):

        hashmap = {}

        for i in range(len(arr)):
            cmp = target - arr[i]

            if cmp in hashmap:
                return True

            hashmap[arr[i]] = i

        return False